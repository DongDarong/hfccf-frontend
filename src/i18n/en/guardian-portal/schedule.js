// Keep schedule copy isolated so the timetable view can stay read-only and
// expand later without changing the summary page wording.
export default {
  schedule: {
    title: 'Schedule summary',
    subtitle: 'Weekly timetable snapshot for the selected child.',
  },
}
